name: CI - Database Migrations

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  test-migrations:
    name: Test Database Migrations
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: openmmo_migration_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable

    - name: Install sqlx-cli
      run: cargo install sqlx-cli --no-default-features --features native-tls,postgres

    - name: Test migrations
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/openmmo_migration_test
      run: |
        sqlx database create
        sqlx migrate run --source migrations
        echo "Migrations applied successfully"

        # Test rollback of last migration
        echo "Testing rollback of last migration..."
        sqlx migrate revert --source migrations
        echo "Rollback successful"

        # Re-apply migrations
        sqlx migrate run --source migrations
        echo "Re-application successful"

        # Check for dangerous operations in migrations
        if find . -name "*.up.sql" -exec grep -l "DROP TABLE" {} \; 2>/dev/null; then
          echo "Warning: DROP TABLE found in up migrations"
        fi

        echo "Migration file validation passed"

    - name: Generate migration info
      env:
        DATABASE_URL: postgresql://postgres:postgres@localhost:5432/openmmo_migration_test
      run: |
        echo "Migration status:"
        sqlx migrate info --source migrations